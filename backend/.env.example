# WARNING: This is an example file. Never use these example values in production.
# Generate your own secure secrets and API keys for production environments.

# Server Configuration
PORT=5000
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:5173

# MongoDB Configuration
MONGO_URI=CHANGE_ME_IN_PRODUCTION

# Redis Configuration (optional, for session storage)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=CHANGE_ME_IN_PRODUCTION

# Session Configuration
SESSION_SECRET=CHANGE_ME_IN_PRODUCTION

# JWT Configuration
JWT_SECRET=CHANGE_ME_IN_PRODUCTION
JWT_REFRESH_SECRET=CHANGE_ME_IN_PRODUCTION
JWT_EXPIRES_IN=24h

# Encryption Configuration
ENCRYPTION_KEY=CHANGE_ME_IN_PRODUCTION

# OpenAI API Configuration
OPENAI_API_KEY=CHANGE_ME_IN_PRODUCTION
OPENAI_MODEL=gpt-3.5-turbo

# API Keys (for backend service authentication)
API_KEY_1=CHANGE_ME_IN_PRODUCTION
API_KEY_2=CHANGE_ME_IN_PRODUCTION
API_KEY_3=CHANGE_ME_IN_PRODUCTION

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173,https://yourdomain.com

# Sentry Configuration (optional, for error tracking)
SENTRY_DSN=CHANGE_ME_IN_PRODUCTION